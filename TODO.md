# AnimeVostTV TODO List

## ⚠️ Несрочно (Баги и доработки текущего)
- [ ] **Ошибка сети:** Добавить ErrorFragment или хотя бы Toast, если Ktor падает с таймаутом (сейчас рутина просто пишет в лог и затихает).

## 📺 Плеер (Media3 / ExoPlayer)
- [ ] **Следующая:** Переход на следующую серию по кнопке
- [ ] **Автовоспроизведение:** Переход на следующую серию после окончания текущей? (сомнительно, так можно весь сериал проспать)
- [ ] **Запоминание позиции:** Интегрировать `AppPrefs.markWatch` глубже в плеер, чтобы позиция сохранялась каждые 10-30 секунд, а не только при выходе(возможно).

## 🏠 Интерфейс (Leanback UI)
- [ ] **Продолжить просмотр:** Создать отдельную строку «Продолжить просмотр» на самом верху, которая берет данные из `watchedEps`, где прогресс > 0 и < 95%.
- [ ] **Голосовой поиск:** Интегрировать стандартный `SearchOrbView` для поддержки голосового ввода Android TV.
- [ ] **Детальный экран:** Добавить отображение скриншотов из `MovieSeriesPageInfo` в горизонтальный список.

## ⚙️ Система и Настройки
- [ ] **Прокси/DNS:** Добавить в настройки возможность смены домена (зеркала) `animevost.org`, если основной заблокирован(может быть актуально в некоторых странах).
- [ ] **Очистка кэша:** Кнопка в настройках для зачистки `AppPrefs` и кеша Glide.

## 🛠 Технический долг (Архитектура)
- [ ] **Модуль потрошения:** Вынести Jsoup-парсеры в отдельные классы-мапперы (сейчас `AnimeVostProvider` слишком большой).
- [ ] **Стабильность:** Заменить блокирующий `CountDownLatch` на современный корутинный аналог StateFlow.

## 🎨 Дизайн и UX
- [ ] **Анимация фона:** Сделать переход фона более плавным (использовать `crossfade` в Glide).

## 🖧 Серверные решения
- [ ] **Proxy/API:** Сделать прокси с кэшированием, который сам будет всё парсить, а клиенту предоставлять простой API (нужна дешёвая серверная инфраструктура или развёртка в локальной сети).
- [ ] **Синхронизация последних:** Сделать синхронизацию последних просмотренных видео(позиций) между устройствами.

